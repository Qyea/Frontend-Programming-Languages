import { Container } from "@mui/material";
import { EditCurrencyInfo } from "../../components/edit-form";

export const CurrencyInfo = () => {
  return (
    <Container>
      <EditCurrencyInfo />
    </Container>
  );
};
